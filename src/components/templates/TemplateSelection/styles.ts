import { SxProps } from '@mui/system';

const selectForm: SxProps = {
  display: 'grid',
  gridTemplateColumns: 'auto 1fr auto',
  gridGap: 8,
};

const styles = { selectForm };

export default styles;
