import { SxProps } from '@mui/system';

const checkboxRow: SxProps = {
  mb: 2,
};

const checkboxIcon: SxProps = {
  pt: 0,
};

const checkboxLabel: SxProps = {
  alignItems: 'start',
};

const styles = { checkboxRow, checkboxIcon, checkboxLabel };

export default styles;
